# easyComment_Sendit_antispam

Интеграция антиспама в **easyComments** на основе поведенческих факторов,
собранных через **SendIt**.

Проект расширяет стандартный функционал easyComments и заменяет стандартный `ec.js`
на кастомную версию с дополнительными возможностями.

---

## 🚀 Установка и подключение

### 1️⃣ JavaScript

В настройках **easyComments** вместо стандартного `ec.js`  
указать данный кастомный скрипт.

---

### 2️⃣ Замена чанков easyComments

Необходимо заменить **3 стандартных чанка** на кастомные и вызвать их следующим образом:

```html
<div class="itemComments">
    <div class="itemHeader">
        <h3>Комментарии</h3>
    </div>

    <div class="wrap">

        {'!ecReplyForm' | snippet: [
            'thread' => 'YurAdres-'~$_modx->resource.id,
            'tplForm' => 'customtpl.ecReplyForm'
        ]}

        {'!ecMessages' | snippet: [
            'votingEnable' => 1,
            'votingAllowGuest' => 1,
            'votingConsiderIP' => 1,
            'limit' => 100,
            'repliesEnable' => 1,
            'thread' => 'YurAdres-'~$_modx->resource.id,
            'sortby' => 'created_on',
            'sortdir' => 'ASC',
            'tpl' => 'custom.tpl.ecMessages'
        ]}

        {'!ecForm' | snippet : [
            'thread' => 'YurAdres-'~$_modx->resource.id,
            'tplForm' => 'customtpl.ecForm'
        ]}

    </div>
</div>


# ✨ Изменения и дополнительные функции
В сравнении со стандартным easyComments:

❌ Удалены рейтинги и загрузка файлов
🧵 Добавлена поддержка мультивложенных ответов
уровни вложенности вычисляются на основе parent_id
автоматически добавляются CSS-классы вложенности
👤 Добавляется имя пользователя, кому именно отвечают
⏱ Отображение времени публикации:
5 минут назад
1 час назад
вчера
старше 2 дней — обычная дата
😀 Добавлены смайлики
💬 Добавлена кнопка цитирования
🔢 Добавлен счётчик длины сообщения
(максимум 1000 символов)
